let i = 1; // Inicializar while

while (i < 10) {
  // Condicion
  if (
    i % 2 === 0
      ? console.log(`${i} es numero par`)
      : console.log(`${i} es numero impar`)
  );
  i++; // Incrementar while
}
